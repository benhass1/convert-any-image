# Shared Contracts

This directory contains framework-neutral data contracts that can be imported by TypeScript packages in the repository. Python models in `local-backend` intentionally mirror their external API shapes rather than importing TypeScript source at runtime.
